/**
 * Localization strings for the UI Multiselect widget
 *
 * @locale en, en-US
 */
$(function() {
    $.extend($.ui.multiselect.locale, {
        addAll: 'Add all',
        removeAll: 'Remove all',
        itemsCount: 'teams selected'
    });
});
